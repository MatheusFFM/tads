export default interface IDate {
  date: string;
  day: number;
  future: boolean;
  hasDay: boolean;
  hasTime: boolean;
  hour: boolean;
  minute: boolean;
  month: number;
  past: boolean;
  present: boolean;
  time: string;
  weekday: number;
  year: number;
}
